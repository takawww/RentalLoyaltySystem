@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect to /login (force reload to reset state)
        Navigation.NavigateTo("/login", forceLoad: true);
    }
}